/- Chapter 3, general theory.

   Now a thin re-export wrapper around `Interpendices.B`. The Ch3
   content (lemma 3.0.1/2, 3.7.2/3 + corollary 3.7.3) all sat at the
   same dependency point as Ch2 content, so it coalesced into B. -/

import Geometry.Theory.Interpendices.B
