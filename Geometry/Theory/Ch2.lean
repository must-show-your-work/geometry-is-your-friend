/- Chapter 2, general theory.

   Now a thin re-export wrapper around `Interpendices.B` (after Prop
   2.1). Kept so existing aggregator imports keep working; eventually
   inline at call sites and delete this. -/

import Geometry.Theory.Interpendices.B
