import Geometry.Theory.Interpendices.B

/-!
Re-export shim during the interpendix migration. Original contents
(`Intersects→∈` Coe instance + `lemma 2.0.15`–`2.0.29` + the `corollary
2.0.25` parallel) have moved to `Geometry/Theory/Interpendices/B.lean`.
-/
